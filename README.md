# Email-Spam-Classifier
This project is an Email Spam Classifier built using machine learning algorithms to classify emails as either spam or not spam (ham). The classifier is designed to detect unsolicited, irrelevant, or potentially harmful emails, enhancing the user experience by filtering out spam messages.
